import React from 'react'

/**
 * ThemeContext — re-exports the shared ThemeProvider from @schoolapex/core.
 * Kept as a thin wrapper for backward compatibility with existing imports.
 */
export { ThemeProvider, useTheme, type Theme, type ThemeContextType } from '@schoolapex/core'
